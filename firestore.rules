rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // [WARNING] These rules are for development/bootstrapping only.
    // They allow anyone to read and write to your database.
    // You should restrict them before going to production with real users.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
